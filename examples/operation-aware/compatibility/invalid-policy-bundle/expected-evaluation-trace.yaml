# Canonical compatibility fixture — scenario: invalid-policy-bundle.
#
# bundle_applicability: null — the failure occurred while validating the
# policy bundle's internal consistency (evaluation phase 2, ADR-0002
# Section 3), before bundle applicability is established (phase 3 onward).
# The bundle is shaped correctly; it fails on a cross-rule invariant
# (duplicate rule_id), so failure_reason is `policy_validation_failure`,
# not `invalid_policy_bundle`. rule_evidence is empty: rule evaluation
# never began. outcome is null, never a substantive decision (ADR-0002
# Section 14).
#
# reason_code is absent: no approved reason-code equivalent for this
# semantic-validation category is published in this repository's governed
# reason-code vocabulary (see docs/reason-code.md and ADR-0003 Section 12's
# illustrative examples, neither of which lists one), so none is invented
# here.

trace_id: trace-compat-invalid-policy-001
request_id: req-compat-invalid-policy-001
evaluation_status: failed
outcome: null
bundle_applicability: null
failure_reason: policy_validation_failure
rule_evidence: []
explanation: The policy bundle failed validation because it declared duplicate rule IDs.
