# vocabulary (placeholder — not yet migrated)

**Contract:** The five canonical action verbs.

**Planned shape (informative, not yet published):** read, write, execute, browse, subscribe.

**Migration order:** #1 of the six planned first contracts. See
[`../../docs/migration-plan.md`](../../docs/migration-plan.md).

**Initial state on publication:** `experimental`. See
[`../../docs/contract-governance.md`](../../docs/contract-governance.md) for the
lifecycle and compatibility rules.

---

This directory is a placeholder. No schema has been migrated into it yet. When
this contract migrates, this directory will publish the machine-readable companion to the action-vocabulary governance rules in basis-architecture. It does not invent verbs; it publishes the controlled set already decided there.

This repository **publishes** decided contracts; it does not invent or redefine
them. The shape above is decided in `basis-architecture` and is reproduced here
only to describe what this directory will eventually hold.
